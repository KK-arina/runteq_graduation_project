<%# app/views/users/new.html.erb %>
<% content_for :title, "新規登録 | HabitFlow" %>

<div class="max-w-md mx-auto px-4 sm:px-6 lg:px-8 py-8">

  <h2 class="text-2xl font-bold mb-6 text-gray-900 text-center">
    新規登録
  </h2>

  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">

    <%# ============================================================ %>
    <%# F-1 追加: Google 登録ボタン %>
    <%# F-2 追加: LINE 登録ボタン %>
    <%# （UX 向上のためフォーム上部に配置） %>
    <%# ============================================================ %>

    <%#
      Google ボタンをフォームより上に置く理由:
        入力不要でワンクリック完了のため、フォームより先に目に入る位置が UX 上最適。
      ENV["GOOGLE_CLIENT_ID"] が未設定の場合はパーシャル内で非表示になる。
    %>
    <%= render "shared/google_auth_button", button_text: "Google アカウントで登録" %>

    <%#
      F-2 追加: LINE 登録ボタン。
      Google ボタンとの間にマージンを設ける（mt-3）。
      ENV["LINE_CHANNEL_ID"] が未設定の場合はパーシャル内で非表示になる。
    %>
    <div class="mt-3">
      <%= render "shared/line_auth_button", button_text: "LINEで登録" %>
    </div>

    <%#
      Google ボタン・LINE ボタンとメールフォームの区切り線。
      少なくとも1つのソーシャルログインが有効な場合のみ表示する。
    %>
    <% if ENV["GOOGLE_CLIENT_ID"].present? || ENV["LINE_CHANNEL_ID"].present? %>
      <div class="relative my-6">
        <div class="absolute inset-0 flex items-center">
          <div class="w-full border-t border-gray-200"></div>
        </div>
        <div class="relative flex justify-center text-sm">
          <span class="px-2 bg-white text-gray-500">またはメールで登録</span>
        </div>
      </div>
    <% end %>

    <%= form_with model: @user,
                  local: true,
                  data: {
                    controller: "form-submit",
                    action: "submit->form-submit#submit"
                  } do |f| %>

      <%= render "shared/form_errors", model: @user %>

      <div class="mb-4">
        <%= f.label :name, "ユーザー名",
            class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_field :name,
            placeholder: "山田太郎",
            class: "w-full px-4 py-2 border border-gray-300 rounded-md
                    focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <div class="mb-4">
        <%= f.label :email, "メールアドレス",
            class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.email_field :email,
            placeholder: "email@example.com",
            class: "w-full px-4 py-2 border border-gray-300 rounded-md
                    focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <div class="mb-4">
        <%= f.label :password, "パスワード",
            class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.password_field :password,
            placeholder: "8文字以上",
            class: "w-full px-4 py-2 border border-gray-300 rounded-md
                    focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <div class="mb-6">
        <%= f.label :password_confirmation, "パスワード（確認）",
            class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.password_field :password_confirmation,
            placeholder: "パスワードを再入力",
            class: "w-full px-4 py-2 border border-gray-300 rounded-md
                    focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <%= f.submit "登録する",
          class: "w-full px-4 py-2 bg-blue-600 text-white rounded-md
                  hover:bg-blue-700 transition font-medium",
          data: {
            "form-submit-target": "button",
            "loading-text": "登録中..."
          } %>
    <% end %>

  </div>

  <div class="mt-4 text-center text-sm text-gray-600">
    すでにアカウントをお持ちの方は
    <%= link_to "ログイン", login_path,
        class: "text-blue-600 hover:text-blue-700 hover:underline" %>
  </div>

</div>