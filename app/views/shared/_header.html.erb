<%# app/views/shared/_header.html.erb %>
<%#
  共通ヘッダー
  全ページで使用される共通のナビゲーションバーです
  Issue #21 で「振り返り」ナビリンクを追加しました
%>

<header class="bg-gray-50 border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <div class="flex justify-between items-center">

      <%# ロゴエリア %>
      <div class="text-2xl font-bold text-blue-600">
        <%= link_to "HabitFlow", root_path, class: "hover:text-blue-700 transition-colors" %>
      </div>

      <%# ナビゲーションエリア %>
      <div class="flex items-center space-x-4">
        <% if logged_in? %>
          <%# ログイン中のナビゲーションリンク %>

          <%# ダッシュボードリンク %>
          <%= link_to "ダッシュボード", dashboard_path,
              class: "text-gray-600 hover:text-gray-900 px-3 py-2
                      text-sm font-medium transition-colors" %>

          <%# 習慣一覧リンク %>
          <%= link_to "習慣一覧", habits_path,
              class: "text-gray-600 hover:text-gray-900 px-3 py-2
                      text-sm font-medium transition-colors" %>

          <%# 週次振り返りリンク（Issue #21 で追加）
              weekly_reflections_path は routes.rb の
              resources :weekly_reflections が生成するパスです
              GET /weekly_reflections にアクセスします %>
          <%= link_to "振り返り", weekly_reflections_path,
              class: "text-gray-600 hover:text-gray-900 px-3 py-2
                      text-sm font-medium transition-colors" %>

          <%# ユーザー名表示 %>
          <span class="text-gray-700 text-sm font-medium">
            <%= current_user.name %> さん
          </span>

          <%# ログアウトボタン
              data-turbo-confirm で削除前に確認ダイアログを表示します
              method: :delete で DELETE /logout にリクエストを送ります %>
          <%= button_to "ログアウト", logout_path,
              method: :delete,
              class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white
                      border border-gray-300 rounded-md hover:bg-gray-50
                      transition-colors",
              data: { turbo_confirm: "ログアウトしますか？" } %>

        <% else %>
          <%# 未ログイン時のナビゲーション %>
          <%= link_to "ログイン", login_path,
              class: "text-gray-600 hover:text-gray-900 px-3 py-2
                      text-sm font-medium transition-colors" %>
          <%= link_to "新規登録", new_user_path,
              class: "px-4 py-2 text-sm font-medium text-white bg-blue-600
                      rounded-md hover:bg-blue-700 transition-colors" %>
        <% end %>
      </div>
    </div>
  </div>
</header>